<!DOCTYPE html>
<html>
<head>
  <title><%=full_title(yield(:title))%></title>

<%=render 'layouts/shim' %>  <!--all the javascript initializing bullshit-->

</head>
<body>
  <%= render 'layouts/header' %> <!-- links to about, home, contact-->

<div class="container">
<%= yield %>
<%= render 'layouts/footer' %>
<%= debug(params) if Rails.env.development? %>
</div>
</body>
</html>
